$('#currentest_cart').html("<%= j (render(:partial => 'shopping_carts/modal', :locals => {:current_cart => @current_cart})).html_safe %>");
$('#item_<%= @cart_item.id %>').fadeOut();
numItems = $('span#cartlength').text();
newNum = numItems - 1;
$('span#cartlength').text(newNum);


// $('#my_cart_total').text('$<%= @total %>');
var total = $('#my_cart_total');
total.text('<%= @total %>');
var newTotal = Number(total.text()).toFixed(2);
total.text('$' + newTotal);